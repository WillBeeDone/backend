spring:
  application:
    name: WillBeeDone

  datasource:
    url: jdbc:mysql://localhost:3306/willbeedone
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: sql77777

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

  liquibase:
    enabled: false

server:
  servlet:
    context-path: /api